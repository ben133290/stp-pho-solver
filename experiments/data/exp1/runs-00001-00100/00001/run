#! /usr/bin/env python

import logging
import os
import platform

from lab.calls.call import Call
from lab import tools

tools.configure_logging()

logging.info(f"node: {platform.node()}")

run_log = open("run.log", "w")
run_err = open("run.err", "w", buffering=1)  # line buffering
redirects = {"stdout": run_log, "stderr": run_err}

# Make sure we're in the run directory.
os.chdir(os.path.dirname(os.path.abspath(__file__)))

Call(['../../cmake-build-debug', '-s 1 2 6 3 5 10 0 7 4 8 9 11 12 13 14 15 -p 0 1 2 3 4 5 -p 0 6 7 8 9 10 -p 0 11 12 13 14 15'], hard_stderr_limit=10240, hard_stdout_limit=10240, memory_limit=2048, name='solve', soft_stderr_limit=64, soft_stdout_limit=1024, time_limit=1800, **redirects).wait()


for f in [run_log, run_err]:
    f.close()
    if os.path.getsize(f.name) == 0:
        os.remove(f.name)
